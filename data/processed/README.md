## Processed Dataset

The processed dataset (`.npz`) used in this project is not included in this repository
due to GitHub file size limits.

Dataset details:
- Format: NumPy `.npz`
- Image resolution: 100 × 100
- Classes: Spiral, Elliptical
- Balanced using SMOTE

The dataset was generated using the scripts in `scripts/` and can be reproduced
by following the preprocessing pipeline.
